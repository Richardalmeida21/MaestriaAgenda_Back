# Banco de dados via variáveis de ambiente (Render)
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA e Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# HikariCP (conexão) - OTIMIZADO PARA PERFORMANCE
spring.datasource.hikari.maximumPoolSize=10
spring.datasource.hikari.minimumIdle=2
spring.datasource.hikari.connectionTimeout=30000
spring.datasource.hikari.idleTimeout=600000
spring.datasource.hikari.maxLifetime=1800000
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# Outros
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}
server.port=8081
server.address=0.0.0.0
jwt.secret=${JWT_SECRET:Y3i49Jx8nQw3sP@74LkF9dC4mJ1N2PZz}

# Comissão removida - agora cada serviço tem sua própria comissão

# WhatsApp via variáveis também (opcional, para seguir padrão)

# Logs - OTIMIZADO PARA PRODUÇÃO
logging.level.root=WARN
logging.level.com.maestria.agenda=INFO
logging.level.org.hibernate=ERROR
logging.level.com.zaxxer.hikari=WARN
logging.level.org.postgresql=WARN
logging.level.org.springframework.jdbc=WARN
logging.level.com.maestria.agenda.config=INFO

# Provedor de Notificação (wppconnect ou whatsapp_cloud_api)

