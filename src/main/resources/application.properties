# Banco de dados via variáveis de ambiente (Render)
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA e Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# HikariCP (conexão)
spring.datasource.hikari.maximumPoolSize=1
spring.datasource.hikari.minimumIdle=1
spring.datasource.hikari.connectionTimeout=30000
spring.datasource.hikari.data-source-properties.prepareThreshold=0

# Outros
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}
server.port=8081
server.address=0.0.0.0
jwt.secret=${JWT_SECRET:Y3i49Jx8nQw3sP@74LkF9dC4mJ1N2PZz}
comissao.percentual=${COMISSAO_PERCENTUAL:70.0}

# WhatsApp via variáveis também (opcional, para seguir padrão)

# Logs
logging.level.org.hibernate=ERROR
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.postgresql=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.com.maestria.agenda.config=DEBUG

# Provedor de Notificação (wppconnect ou whatsapp_cloud_api)

